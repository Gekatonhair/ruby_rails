h1
  | Редактирование
= render 'form', post: @post, buttonValue: :"Обновить новость"
.btn-group
  - if @post.user == current_user
    = link_to 'Смотреть', @post, :class => "btn btn-sm btn-outline-success"
    = link_to "Удалить", post_path(@post), method: :delete, data: { confirm: 'Are you certain you want to delete this?' }, :class => "btn btn-sm btn-outline-danger"
  = link_to 'Назад', posts_path, :class => "btn btn-sm btn-outline-secondary"
